//seeding ingredientsdata in format of Ingredient Schema
const Ingredient = require('../models/ingredient')
const ingredientData = require('./ingredients.json')
//seeding spiaceData in format of Spice Schema
const Spice = require('../models/spice')
const spiceData = require("./spices.json")

// Here we write code to seed the data
// Spice.insertMany(spiceData)
// Ingredient.insertMany(ingredientData)





